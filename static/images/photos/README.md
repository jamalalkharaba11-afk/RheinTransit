RheinTransit uses real photos hosted on Unsplash for the public website.

The photo URLs are defined in app.py and templates/index.html.
They are loaded directly from the internet so the GitHub/Render deployment does not need binary image files.
